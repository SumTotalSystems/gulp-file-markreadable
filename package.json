{
  "name": "gulp-file-markreadable",
  "version": "1.0.0",
  "description": "Makes a file readable my removing the readonly bit.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Gardner <mgardner@sumtotalsystems.com",
  "license": "MIT",
  "dependencies": {
    "through2": "^2.0.0",
    "winattr": "^1.1.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0"
  }
}
